#include <stdio.h>
/**
 * main - entry point
 *
 * Description: using the main function
 * this project prints "all single digits of base 10
 * Return: always 0
 */

int main(void)
{
	int i = 0;

		while (i <= 9)
		{
			printf("%d", i);
			i++;
		}
	printf("\n");
	return (0);
}
